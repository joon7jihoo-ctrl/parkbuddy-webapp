# ParkBuddy Web 개발일지

작성일: 2026-05-31

## 1. 프로젝트 목적

ParkBuddy Web은 파크골프 동호회 운영자가 회원관리, 라운딩 생성, 참가자 선택, 조편성, 점수 입력, 순위표, 라운딩 기록 저장 흐름을 웹에서 빠르게 사용할 수 있도록 만든 React MVP입니다.

기존 ParkBuddy Android 앱에서 논의했던 기능 흐름을 웹앱 형태로 옮긴 버전이며, 현재는 단일 화면 앱처럼 `src/main.jsx`에서 상태와 화면 전환을 관리합니다.

## 2. 현재 기술 구조

- 빌드 도구: Vite
- UI 프레임워크: React
- 아이콘: lucide-react
- 주요 코드: `src/main.jsx`
- 주요 스타일: `src/styles.css`
- 저장 방식: React `useState` 메모리 상태

Supabase 환경변수가 설정되어 있으면 회원, 라운딩 기록, 최근 장소를 Supabase에 자동 저장합니다. 환경변수가 없으면 기존처럼 `useState` 메모리 상태로만 동작하므로 새로고침하면 데이터가 초기화됩니다.

## 3. 참고 문서 기준으로 확인한 구현 상태

참고한 문서:

- `ParkBuddy_conversation_summary.txt`
- `경기방식.txt`

현재 구현된 주요 기능:

- 홈 화면
- 회원 등록, 수정, 삭제
- 회원별 동호회 이름, 직책 입력
- 연락처 자동 하이픈 처리
- 이름, 성별, 연락처 필수 검사
- CSV 회원 양식 다운로드
- CSV 회원 일괄등록
- 라운딩 생성
- 도/광역시 선택 후 파크골프장 선택
- 라운딩별 홀별 규정타수 입력
- 참가자 선택
- 조장 후보 선택
- 조편성
- 홀별 점수 입력
- 규정타수를 점수 입력 기본값으로 사용
- 스트로크 플레이 및 신페리오 순위 계산
- 신페리오 숨김 홀 자동 선정
- 라운딩 기록 저장 및 목록 보기

## 4. 경기 방식 반영 상태

`경기방식.txt` 기준으로 라운딩 생성 화면의 점수/경기 방식 목록은 아래처럼 구성되어 있습니다.

- 스트로크 플레이
- 신페리오
- 매치 플레이
- 포섬
- 포볼

샷건은 점수 계산 방식이 아니라 출발/운영 방식에 가까우므로 현재 선택 목록에서 제외되어 있습니다. 이 방향은 이전 논의의 최종 결론과 맞습니다.

현재 계산 연결 상태:

- 스트로크 플레이: 총타수 기준 순위
- 신페리오: 임시 핸디캡 계산 후 최종 점수 기준 순위
- 매치 플레이, 포섬, 포볼: 선택은 가능하지만 아직 실제 규칙 계산은 없고 총타수 기준으로 처리됨

## 5. 현재 코드에서 발견한 개선 포인트

우선순위가 높은 항목:

1. 회원 수정 시 중복 이름 처리 보완 - 완료
   - 신규 등록은 중복 이름 자동 정리 로직이 있음
   - 수정도 같은 이름 그룹을 자동 번호로 재정리하도록 반영함

2. 신페리오 숨김 홀 방식 개선 - 자동 선정 완료
   - 기존 앞에서부터 2/3 홀을 쓰던 방식을 제거함
   - 9홀 단위로 6개 숨김 홀을 자동 선정함
   - 숨김 홀 파 합계가 해당 구간 기준파의 2/3와 정확히 맞는 조합을 우선 선택함
   - 홀별 규정타수 합계를 기준파로 사용해 신페리오 핸디캡을 계산함

3. 데이터 영구 저장 - Supabase MVP 저장 완료
   - 회원, 라운딩 기록, 최근 장소를 Supabase JSON 상태 테이블에 자동 저장함
   - Supabase 환경변수가 없으면 기존 메모리 상태로 동작함

4. CSV 파싱 안정성 개선 - 완료
   - 쉼표 기준 단순 분리 방식을 제거함
   - 따옴표, 쉼표 포함 값, 줄바꿈을 처리하는 CSV 파서를 적용함
   - 잘못된 핸디캡 값은 `NaN`으로 저장하지 않고 0으로 자동 보정하며 안내함

5. 매치 플레이, 포섬, 포볼 실제 규칙 반영
   - 현재는 방식 선택지만 있고 계산 로직은 스트로크 플레이와 거의 같음
   - 팀전 점수 구조를 별도로 설계해야 함

## 6. 추천 개발 순서

1. Supabase 저장 적용 - 완료
   - 회원, 최근 장소, 라운딩 기록부터 저장
   - 점수 입력 중 새로고침 복구까지 할지는 이후 결정

2. 회원관리 안정화
   - 수정 시 중복 이름 처리
   - 회원 검색
   - 삭제 시 참가자/점수/기록 영향 안내 강화

3. 신페리오 기능 개선
   - 숨김 홀 자동 선택
   - 운영자 직접 지정
   - 숨김 홀 표시 여부 선택

4. 라운딩 기록 강화
   - 기록 상세 보기
   - 기록 삭제
   - CSV 또는 이미지 내보내기

5. 경기 방식 확장
   - 매치 플레이는 홀별 승패 구조 필요
   - 포섬/포볼은 개인 점수 입력 구조와 팀 점수 계산 구조를 분리해야 함

6. 파크골프장 목록 관리
   - 코드 내부 목록을 JSON/CSV로 분리
   - 사용자 직접 추가/수정 지원
   - 추후 지도 API 연동 검토

## 7. 새 개발자가 먼저 볼 파일

- `README.md`: 실행 방법과 포함 기능 요약
- `src/main.jsx`: 화면, 상태, 계산 로직 전체
- `src/styles.css`: 앱 전체 스타일
- `dev/development-log.md`: 현재 개발 방향과 남은 작업

## 8. Git 관리 방향

이 프로젝트는 이제 Git으로 추적할 수 있도록 준비합니다.

권장 커밋 흐름:

```bash
git status
git add .
git commit -m "작업 내용 요약"
```

다른 장소에서도 수정하려면 GitHub 같은 원격 저장소를 만든 뒤 아래 흐름으로 연결합니다.

```bash
git remote add origin 원격저장소주소
git branch -M main
git push -u origin main
```

원격 저장소 주소는 사용자의 GitHub 계정에서 새 저장소를 만든 뒤 복사해서 넣으면 됩니다.

## 9. 2026-06-01 조편성 고도화 작업

이번 작업 목표:

- 기존 단순 랜덤 조편성을 경기 방식별 조편성으로 확장
- 개인전과 팀전 구조 분리
- 실력 균형, 조장 분산, 직전 조 중복 최소화 기준 반영
- 조편성 로직을 UI 컴포넌트에서 분리

추가 파일:

- `src/services/teamAssignment.js`

주요 함수:

- `calculateSkillScore(member, stats)`
- `createBalancedIndividualTeams(participants, options)`
- `createFoursomeTeams(participants, options)`
- `createFourBallTeams(participants, options)`
- `calculatePreviousOverlapScore(candidateTeams, previousRoundTeams)`
- `selectBestTeamAssignment(candidates)`

실력 점수 기준:

- 평균 타수 점수: 50%
- 참가 횟수 점수: 30%
- 실력 등급 점수: 20%

평균 타수는 낮을수록 높은 실력으로 보고, 참가 횟수는 많을수록 경험이 높은 것으로 봅니다. 기존 회원에 평균 타수와 참가 횟수가 없어도 오류가 나지 않도록 기본값을 적용합니다.

개인전 처리:

- 스트로크 플레이
- 신페리오
- 매치 플레이

개인전은 실력 점수 높은 순서로 정렬한 뒤 지그재그 방식으로 조에 배치합니다. 직전 조 중복 최소화 옵션을 선택하면 여러 후보 조편성을 만들고, 조별 실력 편차와 직전 조 중복, 조장 후보 분포를 평가해 가장 나은 후보를 선택합니다.

팀전 처리:

- 포섬
- 포볼

포섬은 비슷한 실력끼리 2인 팀을 만들고, 포볼은 강자와 약자를 짝지어 2인 팀을 만듭니다. 이후 2개 팀을 하나의 조로 배치합니다.

UI 변경:

- 참가자 선택 화면에 조편성 방식 선택 카드 추가
- 개인전은 랜덤, 실력 균형, 조장 기준, 실력 균형 + 직전 조 중복 최소화 제공
- 포섬/포볼은 포섬 추천 팀 편성, 포볼 추천 팀 편성, 실력 균형 + 직전 조 중복 최소화 제공
- 조편성 결과 화면에서 개인전 조와 팀전 조 표시를 구분

기록 변경:

- 라운딩 기록 저장 시 조편성 방식과 조/팀 구조도 함께 저장
- 다음 라운딩 조편성 시 가장 최근의 저장된 조 정보를 이용해 직전 조 중복을 줄임

## 10. 2026-06-01 화면 캡처 검증

사용자 요청에 따라 주요 화면 흐름별 스크린샷을 `C:\Capture` 폴더에 생성하고 확인했습니다.

생성된 캡처:

- `01-home.png`
- `02-members.png`
- `03-round-create-stroke.png`
- `04-member-select-stroke.png`
- `05-team-result-individual.png`
- `06-score-input.png`
- `07-ranking.png`
- `08-records.png`
- `09-round-create-포섬.png`
- `09-member-select-포섬.png`
- `09-team-result-포섬.png`
- `12-round-create-포볼.png`
- `12-member-select-포볼.png`
- `12-team-result-포볼.png`

검증 중 발견하고 수정한 내용:

1. 라운딩 생성 기본 날짜가 UTC 기준으로 잡혀 한국 시간보다 하루 이전 날짜가 표시되는 문제를 수정했습니다.
   - `new Date().toISOString().slice(0, 10)` 대신 `Asia/Seoul` 기준 날짜 문자열을 사용합니다.

2. 점수 입력 화면에서 하단 버튼 영역이 점수 입력 카드 위에 겹쳐 보이는 문제를 수정했습니다.
   - `.bottom-actions`의 sticky 고정을 제거해 입력 필드를 덮지 않도록 했습니다.

3. 반복 검증을 위해 `dev/capture-flow.mjs`를 추가했습니다.
   - Vite 서버와 Chrome headless를 실행합니다.
   - 홈, 회원관리, 라운딩 생성, 참가자 선택, 조편성 결과, 점수 입력, 순위표, 기록 보기, 포섬/포볼 팀전 결과 화면을 자동 캡처합니다.
   - 캡처 파일은 `C:\Capture`에 저장하고 임시 Chrome 프로필은 실행 후 정리합니다.

검증 결과:

- 개인전 조편성 결과 표시 정상
- 포섬 팀전 결과 표시 정상
- 포볼 팀전 결과 표시 정상
- 날짜 표시 정상
- 점수 입력 화면 버튼 겹침 해소

## 11. 2026-06-01 배포 설정

GitHub Pages 자동 배포를 위해 아래 파일을 추가했습니다.

- `.github/workflows/deploy.yml`
- `vite.config.js`

배포 방식:

- `main` 브랜치에 푸시
- GitHub Actions에서 `npm ci` 실행
- `GITHUB_PAGES=true npm run build` 실행
- `dist` 폴더를 `gh-pages` 브랜치에 게시

배포 주소:

```text
https://joon7jihoo-ctrl.github.io/parkbuddy-webapp/
```

Vite는 GitHub Pages 배포 시 `/parkbuddy-webapp/` 경로를 기준으로 asset URL을 생성하도록 설정했습니다. 로컬 개발/일반 로컬 빌드는 기존처럼 `/` 기준으로 동작합니다.

초기 GitHub Pages Actions 배포는 `Configure Pages` 단계에서 실패했습니다. 저장소 Pages 설정이 아직 GitHub Actions 배포 모드로 활성화되지 않은 경우를 피하기 위해, `peaceiris/actions-gh-pages`로 `gh-pages` 브랜치에 정적 파일을 게시하는 방식으로 변경했습니다.

## 12. 2026-06-01 회원 수정 중복 이름 처리

개선 포인트 1번을 반영했습니다.

- 회원 수정 저장 시에도 중복 이름 자동 정리 로직을 적용합니다.
- 예를 들어 기존 회원 이름과 같은 이름으로 수정하면 같은 이름 그룹이 `이름1`, `이름2` 형식으로 재정리됩니다.
- 중복 이름이 없을 때는 기존 회원 목록 위치를 유지한 채 수정 내용만 반영합니다.

## 13. 2026-06-01 회원 동호회/직책 정보 추가

추후 여러 동호회와 로그인 권한 관리를 붙이기 위한 기본 회원 필드를 추가했습니다.

- 회원 데이터에 `clubName`, `position` 필드를 추가했습니다.
- 회원 등록/수정 화면에서 동호회 이름과 직책을 입력할 수 있습니다.
- 회원 목록과 참가자 선택 카드에서 소속 동호회와 직책을 확인할 수 있습니다.
- CSV 회원 양식에 `동호회`, `직책` 열을 추가했습니다.
- 예전 6열 CSV 양식도 계속 읽을 수 있도록 CSV 업로드는 헤더 이름 기준으로 값을 찾습니다.

향후 권한 계획:

- 관리자는 모든 동호회를 조회/관리할 수 있도록 합니다.
- 일반 사용자는 본인이 가입한 동호회만 볼 수 있도록 합니다.
- 로그인 기능을 추가할 때 회원의 `clubName`은 동호회 테이블 또는 동호회 ID로 분리하는 방향이 좋습니다.

## 14. 2026-06-01 홀별 규정타수와 신페리오 자동 숨김 홀

개선 포인트 2번을 반영하면서 라운딩 생성, 점수 입력, 순위 계산 흐름을 함께 수정했습니다.

- 라운딩 생성 화면에서 홀별 규정타수를 입력할 수 있습니다.
- 기본 규정타수는 각 홀 4타이며, 홀 수를 9/18/27/36으로 바꾸면 기존 입력값을 최대한 유지하고 나머지는 기본값으로 채웁니다.
- 점수 입력 화면의 기본값은 라운딩 생성 시 입력한 홀별 규정타수로 채웁니다.
- 점수 입력 화면에는 각 홀의 규정타수도 함께 표시합니다.
- 신페리오 방식 선택 시 라운딩 생성 저장 시점에 숨김 홀을 자동 선정합니다.
- 숨김 홀은 9홀 단위로 6개를 선정합니다.
- 각 9홀 구간의 숨김 홀 파 합계가 해당 구간 기준파의 2/3와 정확히 맞는 조합을 우선 선택합니다.
- 같은 조건의 후보가 여러 개면 파 구성 비율과 3홀 구간 분산이 더 고른 후보를 선택합니다.
- 정확한 파 합계 조합이 불가능한 규정타수 구성에서는 가장 가까운 후보를 선택하고, 순위표와 기록 화면에 `가장 가까운 후보`로 표시합니다.
- 신페리오 핸디캡은 `숨김 홀 점수 합계 * 전체 기준파 / 숨김 홀 기준파 - 전체 기준파`에 0.8을 곱해 계산합니다.
- 순위표와 기록 화면에서 신페리오 숨김 홀, 숨김 파 합계, 목표 파 합계, 규칙 일치 여부를 확인할 수 있습니다.

## 15. 2026-06-01 Supabase 저장 적용

개선 포인트 3번을 Supabase 기반으로 반영했습니다.

- `@supabase/supabase-js` 패키지를 추가했습니다.
- `src/services/supabaseStorage.js`를 추가해 앱 상태를 Supabase에 불러오기/저장할 수 있도록 했습니다.
- Supabase 환경변수는 `.env.example`에 정리했습니다.
- Supabase SQL 스키마는 `supabase/schema.sql`에 추가했습니다.
- 저장 대상은 회원 목록, 라운딩 기록, 최근 사용 장소입니다.
- 앱 시작 시 Supabase에서 저장 데이터를 불러옵니다.
- 데이터 변경 시 0.6초 지연 후 자동 저장합니다.
- Supabase 환경변수가 없거나 로드에 실패하면 기존 메모리 상태로 계속 사용할 수 있습니다.
- 홈 화면에서 현재 저장 상태를 확인할 수 있습니다.

현재 Supabase 스키마는 로그인 전 MVP 용도로 `parkbuddy_app_state` 단일 JSON 상태 테이블을 사용합니다. 추후 로그인과 여러 동호회 권한 관리를 적용할 때는 `clubs`, `club_memberships`, `members`, `round_records` 같은 정규 테이블로 분리하고, 관리자/일반 사용자 권한에 맞는 RLS 정책으로 교체하는 것이 좋습니다.

## 16. 2026-06-02 Supabase 프로젝트 연결

제공받은 Supabase URL과 anon key를 로컬 `.env`에 적용했습니다.

- `.env`는 `.gitignore`에 포함되어 있어 Git 추적 대상에서 제외됩니다.
- Supabase REST API로 `parkbuddy_app_state` 테이블 접근을 확인했습니다.
- `state_key = default` 행 조회가 정상 응답했습니다.
- GitHub Pages 빌드에서도 Supabase 환경변수를 사용할 수 있도록 `.github/workflows/deploy.yml`의 Build 단계에 GitHub Actions secrets 연결을 추가했습니다.
- 로컬 빌드 검증을 완료했습니다.

GitHub CLI가 현재 환경에 설치되어 있지 않아 GitHub Actions secrets는 대시보드에서 직접 등록해야 합니다.

## 17. 2026-06-02 CSV 파싱 안정성 개선

개선 포인트 4번을 반영했습니다.

- 회원 CSV 업로드에서 `line.split(',')` 방식의 단순 분리를 제거했습니다.
- 따옴표로 감싼 값 안의 쉼표를 정상 처리합니다.
- 따옴표 안의 줄바꿈과 이스케이프된 큰따옴표(`""`)를 처리합니다.
- UTF-8 BOM이 포함된 CSV도 첫 헤더를 정상 인식합니다.
- 잘못된 핸디캡 값은 `NaN`으로 저장하지 않고 0으로 보정합니다.
- CSV 업로드 결과 메시지에서 건너뛴 행과 자동 보정 항목을 분리해 보여줍니다.

## 18. 2026-06-02 CSV 연락처 형식 개선

회원 CSV 양식과 업로드 검증의 연락처 형식을 보완했습니다.

- CSV 샘플의 연락처를 `01011110001` 같은 숫자-only 형태에서 `010-1111-0001` 형태로 변경했습니다.
- Excel 등에서 CSV를 열 때 연락처 앞자리 `0`이 사라지는 문제를 줄이기 위한 변경입니다.
- CSV 일괄등록은 하이픈 포함 연락처를 그대로 입력받아 앱 내부에 `010-1111-0001` 형태로 저장합니다.
- 앞자리 `0`이 빠진 `1011110001` 같은 값은 잘못된 연락처로 보고 건너뜁니다.
- 직접 등록 화면의 연락처 안내 placeholder도 `010-1234-5678` 형태로 변경했습니다.

## 19. 2026-06-02 CSV 한글 인코딩 자동 판별

사용자가 제공한 CSV 파일에서 한글 헤더와 성별 값이 깨져 일괄등록이 실패하는 문제를 확인했습니다.

- 원인은 CSV가 UTF-8이 아니라 Windows 한글 인코딩(CP949/EUC-KR)로 저장된 상태였기 때문입니다.
- 기존 업로드는 `FileReader.readAsText(file, 'utf-8')`로만 읽어서 `이름`, `성별`, `남성`, `여성` 값이 깨졌습니다.
- CSV 업로드를 `readAsArrayBuffer`로 변경했습니다.
- UTF-8과 EUC-KR/CP949 후보로 각각 디코딩한 뒤 `이름`, `성별`, `연락처` 헤더 인식 점수가 높은 쪽을 자동 선택합니다.
- UTF-8이 아닌 파일로 읽은 경우 업로드 확인 메시지에 파일 인코딩 안내를 표시합니다.
